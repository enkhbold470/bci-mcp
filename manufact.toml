# Manufact Cloud (formerly mcp-use) deployment hints.
#
# Recommended dashboard settings:
#   Port: 8000
#   Build command: (empty — auto-buildpack runs `uv sync --frozen`)
#   Start command: uvicorn src.server:app --host 0.0.0.0 --port 8000
#
# Alternative start command:
#   sh scripts/manufact-start.sh

[deploy]
port = 8000
start_command = "uvicorn src.server:app --host 0.0.0.0 --port 8000"
